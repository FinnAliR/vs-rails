namespace VSRails;

/// <summary>The autonomous "smart" cart (Jonas model). Stand-in for now — inherits the base rail
/// behaviour; will later gain NPC routing, temporal-gear power and self chunk-loading instead of riding.</summary>
public class EntitySmartCart : EntityRailCart
{
}
